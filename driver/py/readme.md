## Linux
```
!bash:
sudo usermod -aG input $USER   # required for ydotool (re-login after)
pip install -r requirements.txt
python driver.py
```

## Windows
pip install -r requirements.txt
python driver.py